# Optional image tag override (defaults to v0.4.0 in example compose files)
CHUNKFORGE_VERSION=v0.4.0

# Portal exposure
CHUNKFORGE_PORT=8080

# Optional, comma-separated allowed CORS origins for portal API/browser usage
# CHUNKFORGE_ALLOWED_ORIGINS=https://panel.example.com,https://app.example.com

# Node connection settings
CHUNKFORGE_PORTAL_URL=http://chunkforge-portal:8080
CHUNKFORGE_PAIRING_PIN=USE2-31FX
CHUNKFORGE_NODE_NAME=Chunkforge Node
CHUNKFORGE_HEARTBEAT_MS=15000
